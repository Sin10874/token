{
  "version": "2.4.2",
  "coreFeatures": [
    "云端同步超时恢复：事件上传默认使用 500 条小批次，遇到 PostgreSQL statement timeout 时只拆分当前批次并安全重试。",
    "同步状态一致性：仅在最后一个成功子批次提交同步游标，其他权限、网络和应用错误保持立即失败。",
    "绑定身份可见：每次同步前校验 Token 并打印同步账户（member_code），身份错了立刻可见。",
    "换绑入口：npx tokend-cli --reauth 清除本地绑定并重新激活；Token 失效时交互环境自动重新激活，daemon 静默模式输出换绑指引。"
  ]
}
